
describe('template spec', () => {
  it('Should let me see a main page with a header that has links to news sections when I navigate to the website', () => {
    cy.visit('http://localhost:5173/')
  })
})